[project]
name = "stl-conversational-maker-api"
version = "0.1.0"
description = "FastAPI backend for STL Conversational Maker"
requires-python = ">=3.11"

[tool.pytest.ini_options]
pythonpath = ["."]
testpaths = ["tests"]

[tool.ruff]
line-length = 100
